server:
  port: 8084
spring:
  application:
    name: AuthService
  jpa:
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql:
          true
  datasource:
    url: jdbc:postgresql://localhost:5432/ziwg_db
    username: pwr
    password: password